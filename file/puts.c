#include <stdio.h>

// NOTE: puts appends a newline character ('\n') automatic.

int main()
{
  char str[] = "hello world"; // no '\n' in string

  puts(str);

  printf("this is printf\n");

  return 0;
}
